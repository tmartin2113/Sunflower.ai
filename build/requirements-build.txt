# Sunflower AI Professional System - Build Requirements
# Version: 6.2
# Python 3.8+ Required

# Core Build Tools
PyInstaller==6.3.0
setuptools>=68.0.0
wheel>=0.41.0

# Platform-Specific Packaging
py2app==0.28.6 ; sys_platform == 'darwin'
py2exe==0.13.0.0 ; sys_platform == 'win32'
pywin32>=305 ; sys_platform == 'win32'

# Cryptography and Security
cryptography==41.0.7
pycryptodome==3.19.0
certifi>=2023.7.22

# Code Signing and Notarization
macholib>=1.16.2 ; sys_platform == 'darwin'
altgraph>=0.17.4

# Build Utilities
Pillow>=10.1.0  # For icon generation
toml>=0.10.2    # For configuration parsing
pyyaml>=6.0.1   # For YAML configs

# Compression and Archive
py7zr>=0.20.6   # For 7z archives
rarfile>=4.1    # For RAR support

# Development Dependencies
black>=23.11.0  # Code formatting
pylint>=3.0.2   # Code linting
mypy>=1.7.0     # Type checking
pytest>=7.4.3   # Testing framework
pytest-cov>=4.1.0  # Coverage reporting

# Documentation Generation
sphinx>=7.2.6
sphinx-rtd-theme>=2.0.0
sphinx-autodoc-typehints>=1.25.2

# Performance Monitoring
psutil>=5.9.6   # System monitoring
memory-profiler>=0.61.0

# Logging and Monitoring
colorlog>=6.8.0
python-json-logger>=2.0.7

# ISO Creation (optional, system tools preferred)
pycdlib>=1.14.0  # Pure Python ISO creation

# USB Device Management
pyusb>=1.2.1 ; sys_platform != 'darwin'
pyserial>=3.5

# Windows-Specific
pywinauto>=0.6.8 ; sys_platform == 'win32'
winshell>=0.6 ; sys_platform == 'win32'
windows-curses>=2.3.2 ; sys_platform == 'win32'

# macOS-Specific  
pyobjc-core>=10.0 ; sys_platform == 'darwin'
pyobjc-framework-Cocoa>=10.0 ; sys_platform == 'darwin'
pyobjc-framework-IOKit>=10.0 ; sys_platform == 'darwin'
rumps>=0.4.0 ; sys_platform == 'darwin'

# Version Control
gitpython>=3.1.40
semantic-version>=2.10.0

# Network (for potential future updates)
requests>=2.31.0
urllib3>=2.1.0

# Data Validation
jsonschema>=4.20.0
marshmallow>=3.20.1

# Progress Indicators
tqdm>=4.66.1
rich>=13.7.0
alive-progress>=3.1.5

# Resource Management
filelock>=3.13.1
portalocker>=2.8.2

# Hash and Checksum
xxhash>=3.4.1
blake3>=0.3.3

# Parallel Processing
joblib>=1.3.2
multiprocess>=0.70.15

# Environment Management
python-dotenv>=1.0.0
environs>=10.3.0

# Binary Analysis
pefile>=2023.2.7 ; sys_platform == 'win32'
macholib>=1.16.2 ; sys_platform == 'darwin'

# Freeze Support
cx-Freeze>=6.15.10  # Alternative to PyInstaller
auto-py-to-exe>=2.42.0  # GUI for PyInstaller

# Build Optimization
nuitka>=1.9.3  # Python compiler
pyarmor>=8.4.2  # Code obfuscation

# Quality Assurance
bandit>=1.7.5  # Security linting
safety>=3.0.0  # Dependency security check
vulture>=2.10  # Dead code detection

# Deployment Verification
pytest-benchmark>=4.0.0
hypothesis>=6.92.1  # Property-based testing

# Optional Enhanced Features
numpy>=1.24.4  # For performance calculations
pandas>=2.1.3  # For build metrics analysis
matplotlib>=3.8.2  # For build report visualization

# Build Reproducibility
pip-tools>=7.3.0
pipdeptree>=2.13.1
pip-audit>=2.6.1

# Cross-Platform Path Handling
pathlib2>=2.3.7
pathtools>=0.1.2

# Configuration Management
configparser>=6.0.0
python-decouple>=3.8

# Time and Scheduling
schedule>=1.2.0
python-dateutil>=2.8.2

# Temporary File Management
tempfile-ext>=1.2.0
atomicwrites>=1.4.1

# CLI Enhancement
click>=8.1.7
typer>=0.9.0
fire>=0.5.0

# Error Handling
tblib>=3.0.0
better-exceptions>=0.3.3

# Resource Bundling
pyinstaller-hooks-contrib>=2023.11

# Platform Detection
distro>=1.8.0 ; sys_platform == 'linux'
platform-utils>=1.5.0

# End of Build Requirements
# Total estimated installation size: ~500MB
# Build time with all dependencies: ~5-10 minutes
